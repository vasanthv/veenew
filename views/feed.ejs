<!doctype html>
<html lang="en">
<%- include('partials/html-head', {title: "Feed"}); -%>

<body>
  <div id="app" class="wrap">
    <%- include('partials/header', {title: "Feed"}); -%>

    <section>
      <% if (posts.length === 0) { %>
      <div class="empty">Nothing here yet. <a href="/follows">Follow someone</a> to see their posts.</div>
      <% } else { %>
      <% posts.forEach((post) => { %>
      <article>
        <div class="flex">
          <div class="flex-1">
            <strong>
              <a href="<%= post.remoteUser.actorUrl %>" target="_blank" rel="noopener">
                <%= post.remoteUser.name || post.remoteUser.handle %>
              </a>
            </strong>
            <span class="small">@<%= post.remoteUser.handle %></span>
          </div>
        </div>
        <div class="content"><%- post.content %></div>
        <% if (Array.isArray(post.imageUrls) && post.imageUrls.length > 0) { %>
        <div class="content">
          <% post.imageUrls.forEach((src) => { %>
          <a href="<%= src %>" target="_blank" rel="noopener"><img src="<%= src %>" alt="" loading="lazy" /></a>
          <% }); %>
        </div>
        <% } %>
        <div class="small">
          <a href="<%= post.url %>" target="_blank" rel="noopener">
            <time datetime="<%= new Date(post.createdOn).toISOString() %>"><%= dayjs(post.createdOn).fromNow() %></time>
          </a>
        </div>
      </article>
      <% }); %>
      <% } %>
    </section>

    <%- include('partials/pagination'); -%>
    <%- include('partials/footer'); -%>
  </div>
</body>

</html>
